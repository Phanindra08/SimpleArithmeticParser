spring:
  application:
    name: SimpleArithmeticParser

  datasource:
    url: jdbc:h2:mem:parserdb
    username: parser
    password: parser
    driver-class-name: org.h2.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always

logging:
  level:
    root: INFO

simple-arithmetic-output: C:\\Users\\skothur1\\Downloads\\SplOutput\\
chunk-size: 5